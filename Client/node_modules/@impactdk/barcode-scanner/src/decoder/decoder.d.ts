import { IBarcode } from './barcode';
export interface IDecoder {
    decode(context: CanvasRenderingContext2D): IBarcode;
}
export declare class WasmDecoder implements IDecoder {
    private static instance;
    private wasmDecoder;
    private worker;
    private constructor();
    static getInstance(installationDir?: string): WasmDecoder;
    static removeInstance(): void;
    decode(context: CanvasRenderingContext2D): IBarcode;
    private dispose;
}
