import { IDecoder } from "../decoder/decoder";
import { IBarcode } from '../decoder/barcode';
export declare class Scanner {
    private videoElement;
    private decoder;
    private cb;
    private decodeCanvas;
    private constraints;
    private running;
    constructor(videoElement: HTMLVideoElement, decoder: IDecoder, cb: (barcode: IBarcode) => void);
    start(): Promise<void>;
    stop(): void;
    private resolve;
    private reject;
    private onLoadedData;
    private getOnFrameHandler;
}
